A database reader, parser, and writer to translate from WPI's Collegescheduler course data to the old WPI scheduler's XML.

See code/README.txt for more information.

Do not add any files to this directory. Everything in this directory except the code directory should be disregarded.

